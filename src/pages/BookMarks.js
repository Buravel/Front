import React from 'react';
import Responsive from '../components/common/Responsive';

const BookMarks = () => {
    return (
        <Responsive>
            <div>bookmarks</div>
        </Responsive>
    );
};

export default BookMarks;
