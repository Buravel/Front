import React from 'react';
import Responsive from '../components/common/Responsive';

const MainPage = () => {
    return (
        <Responsive>
            <div>mainPage</div>
        </Responsive>
    );
};

export default MainPage;
