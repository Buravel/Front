import React from 'react';
import Responsive from '../components/common/Responsive';

const Plan = () => {
    return (
        <Responsive>
            <div>plan</div>
        </Responsive>
    );
};

export default Plan;
